services:
  - type: web
    name: ai-bonds
    env: node
    plan: free
    buildCommand: cd client && npm install && npm run build
    startCommand: node server/index.js
    repo: https://github.com/YOUR_USERNAME/AI-Bonds
